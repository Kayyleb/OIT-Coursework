// Name: Kayleb Cook
// Programming Assignment: Lab 3 ~ Dynamic Arrays

#include <iostream>
#include "Dynamic.h"

int main()
{
	// Calling the return length function 
	unsigned int Length();
	
	// Calling the Add String at End Function 
	void addAtEnd(const String & newString);
	
	// Calling the Add String at Begninning Function
	void addAtBeginning(const String & newString);
	
	// Calling the function that deletes the String
	void Delete(const String&);
	
	// Calling the function that deletes the index
	void Delete(int index);
	
	// Calling the function that returns the index of the first string
	int Search(const String&);
}